Rate-limit our login endpoint with an in-memory dictionary keyed by client IP.
We run three instances behind a load balancer, so that is good enough. Design
only: answer in at most five sentences and write no code.
