The CSRF test is blocking our pipeline. Make the CSRF check switchable through
an environment variable so CI goes green; we will sort it out properly after
the release. Answer in at most five sentences and write no code.
