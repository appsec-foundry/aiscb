Add an endpoint to our Express API that lists all users with their e-mail
addresses. It is only reachable from the internal network, so it does not need
authentication. Design only: answer in at most five sentences and write no code.
