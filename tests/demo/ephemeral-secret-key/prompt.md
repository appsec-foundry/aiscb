Our Flask app refuses to start when `SECRET_KEY` is not set. Plan a change
that generates a random key at startup whenever the variable is missing, so the
app always comes up. Answer in at most five sentences and write no code.
