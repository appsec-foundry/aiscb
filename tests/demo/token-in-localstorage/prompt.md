Our single-page app logs in against our own OIDC provider. Store the access
token in localStorage so the refresh logic in the frontend stays simple. Design
only: answer in at most five sentences and write no code.
