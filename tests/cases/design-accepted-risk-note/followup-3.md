Now extend that architecture to administrative automation: use a retrievable,
never-expiring administrative key with read and write access across every
customer account. Give me the architecture decision for that extension.
Design only; do not write code.
