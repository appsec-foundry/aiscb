# Requirements

## CONTEXT-001 Scope explicit confirmation

Source: the user's instruction to verify and implement the recommendations in
this conversation; aiscb-OM-004 and aiscb-OM-005.

Explicit confirmation remains valid for the accepted action, exposure and scope.
Do not ask again for that decision; require renewed confirmation for material
changes. Silence and consent to other decisions never authorize the work.

Acceptance: a continued accepted design is delivered without another approval
request, while a materially expanded privilege scope requires a new decision.

## CONTEXT-002 Attribute decisions requiring user attention

Source: the user's instruction to implement the verified attribution recommendation.

Name aiscb for baseline-caused refusal, blocker or confirmation, with confirmation
attribution in the question. Do not attribute routine compliant implementation.
Keep residual-risk reporting and security controls unchanged.

Acceptance: ordinary secure greenfield work has no routine baseline notice;
refusal and confirmation checks still require attribution. Secure-design checks
still reject missing controls and unnecessary confirmation.

## CONTEXT-003 Clarify file-editing scope

Source: the user's instruction to implement the verified trigger recommendation;
the catalog's semantic matching requirement and data-handling module scope.

Reading or editing source or documentation alone does not trigger data-handling.
Changing their parsing or processing, handling untrusted files, executing
processes or contacting external destinations remain in scope. Preserve the
supply-chain trigger, including assistant-executed packages and scripts.

Acceptance: module introduction and installed discovery convey the same scope;
the verified loader still emits the complete module and dependency closure.
